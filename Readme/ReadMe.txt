1. Import the eclipse project in workspace or run through commandline
2. Input file has been added to the project already. If you wish to test the code with your own input file,
	edit the input file data/input.txt, enter your values.
	
		NOTE: The input file should be constructed as follows:
				---- customerid,pincode
	It reads only single line			
			  
3. Output is displayed on console.

Note: API CALLS are limited per hour. Thus, it can show some faulty data.


